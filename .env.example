# ========== Frontend (Vite) ==========
# ใช้เมื่อ build/deploy ออนไลน์ — ใส่ URL ของ Backend API (ต้องลงท้าย /api)
# Development: ไม่ต้องตั้ง หรือเว้นว่าง จะใช้ http://localhost:5000/api
# Production: ใส่ URL จริง เช่น https://your-backend.onrender.com/api
VITE_API_URL=http://localhost:5000/api
